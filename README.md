# Fleet Scribe


Python library implementing the One Delta principle. Delta detection with configurable threshold. Persistent cache (3.8x faster than SQLite for writes). Pattern compilation handles 220k events/sec on single core. `pip install fleet-scribe`.


Apache 2.0 — Cocapn fleet infrastructure.
